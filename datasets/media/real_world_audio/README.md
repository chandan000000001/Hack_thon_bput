# Real-world audio gate split

User-contributed ElevenLabs clips (fake/) and voice memos (real/). NEVER used for training — evaluation-only gate split.

Gate metrics reported in ml/models/audio_v1_metrics.json.
